kind = "web"
previewPath = "/"
title = "Habibi Store"
version = "1.0.0"
id = "artifacts/habibi-store"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
localPort = 20461
name = "web"
paths = ["/"]

[services.development]
run = "pnpm --filter @workspace/habibi-store run dev"

[services.env]
PORT = "20461"
BASE_PATH = "/"

[services.production]
build = ["pnpm", "--filter", "@workspace/habibi-store", "run", "build"]
publicDir = "artifacts/habibi-store/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"
